{
  "name": "klucz-web",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --filter @klucz/web dev"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
