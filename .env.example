# Skopiuj do .env  (.env jest w .gitignore i nigdy nie wchodzi do repo)
#
#   Windows PowerShell:  Copy-Item .env.example .env
#   macOS / Linux:       cp .env.example .env

# Port Postgresa na hoście. Wysoki numer NIE gwarantuje, że jest wolny —
# na maszynie deweloperskiej stoi zwykle kilka innych baz w kontenerach.
# Gdy `task up` powie „address already in use", podnieś tę liczbę
# i popraw ją także w DATABASE_URL poniżej.
DB_PORT=55434
DATABASE_URL=postgresql://klucz:klucz_dev@localhost:55434/klucz

# Korzeń blob storage. Ścieżka WZGLĘDNA wobec korzenia repo — w bazie stoją
# ścieżki względne, żeby korpus był przenośny między maszynami.
BLOB_ROOT=data/blob

# Katalog mirrora CKE (2508 plików). Zasada: mirror raz, potem tylko kopia.
MIRROR_ROOT=data/raw
